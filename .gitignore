/target
/.draft/
# Per-machine environment answers; see .envrc.
/.envrc.local

# Slidev deck build state. `package-lock.json` is tracked on purpose — it is
# the pin, and the deploy workflow installs from it with `npm ci`.
/slides/node_modules/
/slides/dist/
/slides/.slidev/
/slides/components.d.ts

# --- cog gitignore (rust) ---
# Rust
/target/
**/*.rs.bk
*.pdb
# OS / editor cruft
.DS_Store
.idea/
.vscode/
# Nix devshell
.direnv/
/result

# --- cog gitignore (rust) ---
rustc-ice-*.txt
**/mutants.out*/
